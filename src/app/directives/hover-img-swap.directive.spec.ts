import { HoverImgSwapDirective } from './hover-img-swap.directive';

describe('HoverImgSwapDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverImgSwapDirective();
    expect(directive).toBeTruthy();
  });
});
