import { Request, Response } from 'express';


export function getCart(req: Request, res: Response) {
  // TO-DO => (functionality) get cart
  // res
  //   .status(200)
  //   //TO-Do => add Interceptor
  //   .json({payload: localStorage.getItem('cart_contents')})
}
